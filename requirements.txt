# Loose lower bounds rather than exact pins: exact pins break on newer Python
# versions that have no wheel for the pinned build, and this has to install on
# whatever interpreter is to hand.
flask>=3.0
gunicorn>=22.0
python-dotenv>=1.0
numpy>=1.26
faiss-cpu>=1.8
pypdf>=5.0
openai>=1.50

# Embeddings. Needs a Python version torch publishes wheels for (3.10-3.13 at
# time of writing). Without these, set EMBEDDING_BACKEND=hashing - the pipeline
# runs, with much worse retrieval.
sentence-transformers>=3.0
langchain-huggingface>=0.1
